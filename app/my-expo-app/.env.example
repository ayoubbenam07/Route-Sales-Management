# PowerSync Cloud instance (already defaults in code if unset)
EXPO_PUBLIC_POWERSYNC_URL=https://6a63612e2320c406bd5882c7.powersync.journeyapps.com

# Required for sync until production JWT auth is configured in PowerSync.
# Generate in PowerSync Dashboard → Client Auth → Development tokens → Connect.
EXPO_PUBLIC_POWERSYNC_DEV_TOKEN=
